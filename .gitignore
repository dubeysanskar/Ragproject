.venv/
__pycache__/
*.pyc
node_modules/
.next/
.env
build.log

# Bench output is regenerated by scripts/bench.py; report.md is committed
# deliberately (it is submission evidence), the raw CSV is not.
bench/latencies.csv

# Derived: rebuild with scripts/build_snapshot.py (19.7 MB of vectors)
data/snapshot/
apps/api/data/
snapshot.log

# Third-party eval loop, vendored per its runbook; not our code
eval/
run.ps1
eval-requirements.txt
results/
scores.jsonl
calib.json
eval-*.log
calib.log
